Scripts in this directory depend on other Lua files in subdiretory(s). They can be run directly, or built into a single file with `build.py main.lua output.lua`.

A pre-built single-file version of MKDS Info can be found at https://tasvideos.org/UserFiles/ForUser/Suuper, although it is not updated very often.
